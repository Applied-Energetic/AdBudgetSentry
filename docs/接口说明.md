# AdBudgetSentry 接口说明

本文档面向后续开发者，说明当前项目中前端油猴脚本与本地后端之间的接口、字段和调用顺序。

## 1. 架构边界

### 油猴脚本负责

- 页面采集
- 本地短期历史快照
- 心跳上报
- 采集错误上报
- 手动请求测试报警

### 本地后端负责

- 接收采集、心跳、错误、告警回执
- 持久化到 SQLite
- 规则分析与大模型总结
- PushPlus 告警发送
- 实例离线巡检
- 连续采集失败巡检
- 后台管理页展示
- 实例健康聚合

### 当前不建议的做法

- 油猴脚本直接读取 SQLite
- 浏览器前端直接访问数据库文件
- 把十分钟前数据对比逻辑建立在“重新抓页面历史值”上

原因很简单：浏览器拿不到本地数据库的稳定读能力，而且前端读 DB 会让权限、路径、并发和安全都变复杂。当前方案是：

- 油猴脚本保留本地短期快照，用于实时上报基线和增量
- 后端保留完整历史，用于后台追溯和后续集中分析

如果未来要做“由后端统一返回十分钟前基线”，推荐新增 HTTP 接口，而不是让脚本直接读数据库。

## 2. 前端到后端接口

默认后端基地址：

```text
http://127.0.0.1:8787
```

### 2.1 `POST /heartbeat`

用途：

- 上报脚本实例还活着
- 汇报最近一次采集状态
- 供后台判断 `green / yellow / red`

请求体示例：

```json
{
  "instance_id": "tm-abc123",
  "account_id": "acc-001",
  "account_name": "测试账号",
  "page_type": "financial",
  "page_url": "https://niu.e.kuaishou.com/financial/record",
  "script_version": "2.3.0",
  "heartbeat_at": 1760000000000,
  "browser_visible": true,
  "capture_status": "success",
  "last_capture_at": 1760000000000,
  "row_count": 12,
  "error_message": null
}
```

关键字段：

- `instance_id`：脚本实例 ID，脚本首次安装后固定
- `capture_status`：`success / warning / error`
- `last_capture_at`：最近一次成功采集时间

### 2.2 `POST /ingest`

用途：

- 上报一次成功采集结果
- 写入 `capture_events`
- 触发规则分析和必要时的大模型分析

请求体示例：

```json
{
  "instance_id": "tm-abc123",
  "account_id": "acc-001",
  "account_name": "测试账号",
  "page_type": "financial",
  "page_url": "https://niu.e.kuaishou.com/financial/record",
  "script_version": "2.3.0",
  "captured_at": 1760000000000,
  "row_count": 12,
  "metrics": {
    "current_spend": 1280.5,
    "increase_amount": 37.0,
    "baseline_spend": 1243.5,
    "compare_interval_min": 10,
    "notify_threshold": 20
  },
  "raw_context": {
    "history_samples": 18,
    "baseline_time": 1759999400000,
    "today": "2026-03-27",
    "ai_enabled": true,
    "ai_provider": "deepseek",
    "business_context": "当前账号名称：测试账号\n当前账号ID：acc-001\n脚本实例ID：tm-abc123"
  }
}
```

关键字段：

- `metrics.current_spend`：当前页面抓到的今日总消耗
- `metrics.increase_amount`：当前点位和对比窗口基线的差值
- `metrics.baseline_spend`：窗口基线值
- `metrics.compare_interval_min`：例如 10 分钟
- `metrics.notify_threshold`：例如 20 元

### 2.3 `POST /error`

用途：

- 上报脚本内部错误
- 不依赖采集成功

请求体示例：

```json
{
  "instance_id": "tm-abc123",
  "occurred_at": 1760000000000,
  "error_type": "capture_error",
  "error_message": "未找到今日消耗数据行",
  "page_url": "https://niu.e.kuaishou.com/financial/record",
  "script_version": "2.3.0"
}
```

### 2.4 `POST /alerts/test`

用途：

- 手动触发后端测试报警
- 当前主要给油猴脚本“测试报警”按钮使用

请求体示例：

```json
{
  "instance_id": "tm-abc123",
  "account_id": "acc-001",
  "account_name": "测试账号",
  "page_type": "financial",
  "page_url": "https://niu.e.kuaishou.com/financial/record",
  "script_version": "2.4.0",
  "current_spend": 1280.5,
  "increase_amount": 37.0,
  "compare_interval_min": 10,
  "baseline_spend": 1243.5,
  "baseline_time": 1759999400000,
  "analysis_text": "结论：测试报警",
  "triggered_at": 1760000000000
}
```

### 2.5 `POST /alert-record`

用途：

- 兼容旧版脚本保留的回执接口
- 新版以后主要由后端内部发送器直接写入数据库

请求体示例：

```json
{
  "instance_id": "tm-abc123",
  "account_id": "acc-001",
  "account_name": "测试账号",
  "page_type": "financial",
  "page_url": "https://niu.e.kuaishou.com/financial/record",
  "script_version": "2.3.0",
  "alert_kind": "threshold",
  "title": "【磁力金牛预警】测试账号 (acc-001)",
  "content_preview": "消耗异常提醒\n账号名称：测试账号\n账号ID：acc-001",
  "channel": "mail",
  "channel_option": "",
  "delivery_provider": "pushplus",
  "send_status": "sent",
  "provider_response": "{\"code\":200}",
  "severity": "medium",
  "anomaly_type": "threshold_breach",
  "triggered_at": 1760000000000
}
```

`send_status` 说明：

- `sent`：发送成功
- `failed`：发送失败
- `skipped`：本次跳过，例如没配 Token

### 2.6 `POST /analyze`

用途：

- 手动调用 AI 分析
- 当前仍保留给油猴脚本“立即分析”按钮使用

这个接口不负责落库实例状态，只负责返回分析结果。

## 3. 后端查询接口

### `GET /health`

- 返回默认 provider 和数据库路径

### `GET /healthz`

- 基础存活检查

### `GET /readyz`

- 数据库就绪检查

### `GET /admin/summary`

- 返回实例总数
- 返回 `green / yellow / red`
- 返回最近心跳、最近采集、分析总数、告警总数

### `GET /admin/instances`

- 返回按实例聚合的健康信息

### `GET /admin/alerts`

- 返回告警中心 HTML 页面
- 支持 `account_keyword / send_status / alert_kind / date_from / date_to`

### `GET /admin/api/alerts`

- 返回告警历史 JSON
- 支持 `limit / account_keyword / send_status / alert_kind / date_from / date_to`

### `GET /admin/alerts/export.csv`

- 导出当前筛选结果 CSV

### `GET /`

- 后台管理页 HTML

## 4. 当前前端内部接口约定

虽然油猴脚本不是 HTTP 服务，但为了便于维护，下面这些函数相当于脚本内部的稳定接口：

- `scrapeTodaySpendWithRetry()`：负责页面采样，带重试
- `appendHistory(spend)`：负责写本地短期历史快照
- `getBaselineRecord()`：负责从本地历史中找对比窗口基线
- `evaluateSpend()`：一次完整采样流程
- `sendHeartbeat()`：上报实例心跳
- `sendIngest()`：上报成功采样
- `sendErrorReport()`：上报脚本错误
- `pushAlert()`：发送 PushPlus 并回写 `/alert-record`

## 5. 推荐调用顺序

### 页面启动后

1. 创建面板
2. 恢复上次成功采样值
3. 执行一次 `evaluateSpend()`
4. 发送一次 `heartbeat`
5. 进入周期轮询

### 周期轮询中

1. 每 60 秒尝试执行 `evaluateSpend()`
2. 每 2 分钟发送 `heartbeat`
3. 到达页面刷新时间后整页刷新

### 单次 `evaluateSpend()` 内部顺序

1. 重试抓取当前总消耗
2. 写入本地历史快照
3. 找到窗口基线
4. 计算 `increaseInWindow`
5. 上报 `POST /ingest`
6. 命中阈值时发 PushPlus
7. 发完后回写 `POST /alert-record`

## 6. 关于“十分钟前对比值”的结论

当前推荐方案：

- 油猴脚本用 `GM_setValue("spend_history")` 保存短期快照
- 取当前时间点和窗口开始点附近的快照做差

不建议当前直接改成“前端读取数据库再计算”，因为：

- 技术上浏览器无法稳定直接读本机 SQLite
- 会让前端依赖后端存储细节
- 一旦切换数据库，前端也会被迫跟着改

如果后续发现“前端本地快照”仍不够稳定，更合理的升级方案是：

1. 后端新增一个历史窗口查询接口
2. 油猴脚本只调用接口
3. 由后端统一返回“当前值、十分钟前值、窗口差值”
